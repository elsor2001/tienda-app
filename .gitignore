# Dependencias
node_modules/

# Archivos de logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
logs
*.log

# Variables de entorno
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Archivos de configuración del sistema
.DS_Store
Thumbs.db

# Archivos de VS Code
.vscode/
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json# Dependencias (esta es la carpeta más pesada y no debe subirse)
node_modules/

# Logs de errores de npm
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Variables de entorno (contienen contraseñas o llaves API)
.env
.env.test
.env.local

# Archivos del sistema operativo
.DS_Store
Thumbs.db

# Configuración de VS Code (opcional, algunos prefieren no subirla)
.vscode/

# Carpetas de construcción/compilación (si usas TypeScript o Babel)
dist/
build/
!.vscode/extensions.json

# Archivos de cobertura de tests
coverage/
.nyc_output/
# Dependencias (esta es la carpeta más pesada y no debe subirse)
node_modules/

# Logs de errores de npm
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Variables de entorno (contienen contraseñas o llaves API)
.env
.env.test
.env.local

# Archivos del sistema operativo
.DS_Store
Thumbs.db

# Configuración de VS Code (opcional, algunos prefieren no subirla)
.vscode/

# Carpetas de construcción/compilación (si usas TypeScript o Babel)
dist/
build/
# Build / producción# Dependencias (esta es la carpeta más pesada y no debe subirse)
node_modules/

# Logs de errores de npm
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Variables de entorno (contienen contraseñas o llaves API)
.env
.env.test
.env.local

# Archivos del sistema operativo
.DS_Store
Thumbs.db

# Configuración de VS Code (opcional, algunos prefieren no subirla)
.vscode/

# Carpetas de construcción/compilación (si usas TypeScript o Babel)
dist/
build/
dist/
build/

# Cache
.cache/
.parcel-cache/

# Archivos temporales
*.tmp
*.temp
*.swp

# Archivos de sistema Linux
*~

# Archivos generados por Node
pids
*.pid
*.seed
*.pid.lock

# TypeScript (si usas TS)
*.tsbuildinfo

# Prisma, Sequelize, etc
prisma/migrations/
# Dependencias (esta es la carpeta más pesada y no debe subirse)
node_modules/

# Logs de errores de npm
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Variables de entorno (contienen contraseñas o llaves API)
.env
.env.test
.env.local

# Archivos del sistema operativo
.DS_Store
Thumbs.db

# Configuración de VS Code (opcional, algunos prefieren no subirla)
.vscode/

# Carpetas de construcción/compilación (si usas TypeScript o Babel)
dist/
build/
package-lock.json